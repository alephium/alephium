version: "3.3"

services:
  alephium_gpu_miner:
    image: alephium/gpu-miner:latest
    depends_on:
      - alephium
    restart: always
    runtime: nvidia
    privileged: true
    command:
      - 'alephium'
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
