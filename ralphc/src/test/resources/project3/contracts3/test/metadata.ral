Contract MetaData() {
    @using(preapprovedAssets = true, assetsInContract = false, updateFields = false)
    pub fn foo() -> () {
        transferAlph!(callerAddress!(), callerAddress!(), 1 alph)
        return
    }

    @using(preapprovedAssets = false, assetsInContract = true, updateFields = false)
    fn bar() -> () {
        transferAlphToSelf!(selfAddress!(), 1 alph)
        return
    }

    @using(updateFields = false)
    fn baz() -> () {
        return
    }
}
