include "platform.conf"

akka.actor.default-dispatcher.mailbox-requirement = "org.alephium.monitoring.MonitoringMailboxSemantics"
akka.actor.mailbox.requirements { "org.alephium.monitoring.MonitoringMailboxSemantics" = monitoring-mailbox }

monitoring-mailbox {
  mailbox-type = "org.alephium.monitoring.MonitoringMailbox"
}

actor {
  debug {
    receive = on
    autoreceive = on
    lifecycle = on
    unhandled = on
  }
}

io.tcp.trace-logging = on
