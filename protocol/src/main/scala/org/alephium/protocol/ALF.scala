package org.alephium.protocol

object ALF {
  //scalastyle:off magic.number
  val MaxALFValue   = BigInt(100000000)
  val CoinBaseValue = BigInt(10) // Note: temporary value
  //scalastyle:on magic.number
}
